# configs/dataset/qm9.yaml

name: QM9

init:
  _target_: src.data.qm9.QM9DataModule
  target: 4  # QM9 targets: mu, alpha, homo, lumo, gap, r2, zpve, u0, u298, h298, g298, cv
  batch_size_train: 32
  batch_size_inference: 64
  num_workers: 4
  splits: [0.6, 0.1, 0.15, 0.15]
  seed: 42
  subset_size: null  # Use null for full dataset or int for subset
  data_augmentation: false
  mode: "semisupervised"
  name: "QM9"
  



